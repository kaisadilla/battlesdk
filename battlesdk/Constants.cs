namespace battlesdk;
public static class Constants {
    /// <summary>
    /// The minimum progress in an character's current move for it to be
    /// interactable.
    /// </summary>
    public const float MOVE_INTERACTION_THRESHOLD = 0.6f;
}
